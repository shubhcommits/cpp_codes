// this does not follow any order and stores unique elements
// Most of the case the TC-> O(1)
// In very rare case the TC-> O(n)
// upper_bound and lower_bound does not works in this
// All others thins are same as set
#include<bits/stdc++.h>
using namespace std;

void unorderedSet(){
  unordered_set<int> st;
}